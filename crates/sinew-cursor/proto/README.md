# agent.v1 protobuf

- `agent.fds` — legacy Buf `fileDesc` export (`node scripts/export-agent-descriptor.mjs`).
- `agent.pb` — standard `google.protobuf.FileDescriptorSet` for `prost-reflect` (`node scripts/agent-bridge/export-agent-fds-prost.mjs`).
